# Use an official OpenJDK runtime as a parent image
FROM openjdk:17-jdk-slim

# Set the working directory inside the container
WORKDIR /app

# Copy the entire contents of the target directory to the container's /app directory
COPY Calculator-App-SPE/target/ /app/

# Rename the JAR file to app.jar (adjust if necessary)
RUN mv /app/Calculator-SPE-MiniProject-1.0-SNAPSHOT-jar-with-dependencies.jar /app/app.jar

# Expose the port the app will be running on
EXPOSE 8080

# Define the command to run the JAR file
ENTRYPOINT ["java", "-jar", "/app/app.jar"]
